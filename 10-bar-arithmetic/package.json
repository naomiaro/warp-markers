{
  "name": "@warp-math/bar-arithmetic",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Bar numbering across meter changes: a running bar count per meter entry, proven equivalent to the production MeterMap",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@dawcore/transport": "^0.0.12",
    "@warp-math/beats-io": "*",
    "@warp-math/grid-follows-file": "*"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
