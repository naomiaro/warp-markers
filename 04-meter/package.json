{
  "name": "@warp-math/meter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The meter layer: bar/position-in-bar arithmetic, independent of tempo.",
  "scripts": {
    "test": "vitest run",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@warp-math/the-math": "*"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
