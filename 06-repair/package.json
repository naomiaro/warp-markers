{
  "name": "@warp-math/repair",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./repair.js",
  "description": "Hand-edit operations for a beat map: insert / delete / move beats, validated against the meter layer.",
  "scripts": {
    "test": "vitest run",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@warp-math/the-math": "*",
    "@warp-math/meter": "*",
    "@warp-math/beats-io": "*"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
